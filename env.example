# Optional overrides. Never put real secret keys in the project ZIP.
PORT=5000
CLERK_ISSUER=https://busy-skylark-3.clerk.accounts.dev

# Comma-separated public website origins. Set this when deploying.
# Example: https://your-game.example.com
CLERK_AUTHORIZED_PARTIES=
ALLOWED_ORIGINS=

# Optional absolute path for persistent SQLite storage.
DB_PATH=
